sort := (array: [int]) -> [int] {
    if std.len(array) < 2 return array;
    first := array[0];
    (array1, array2) := array~\(item:int)->bool{return item < first;};
    (array2, array3) := array2~\(item:int)->bool{return first == item};
    return sort(array1)+array2+sort(array3);
};
